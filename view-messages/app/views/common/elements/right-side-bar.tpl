     {$view->element('most-discussed-topics', $scache)} 
     {$view->element('recent-topics', $scache)}
     {$view->element('recent-posts', $scache)}
     {$view->element('top-posters', $scache)}
     {$view->element('visitor-map', $scache)}
     